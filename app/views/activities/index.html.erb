Welcome! This is the Activities index page

<p>What do you want to do?</p>

<ul>
<% @activities.each do |activity| %>
<li><%= link_to activity.activity_name, activity_trips_path(activity.id) %></li>
<% end %>
</ul>

<button><%=link_to "Create a new trip", new_trip_path%></button>

<% if user_signed_in? && current_user %>
<button>
  <%= link_to "Edit my profile", edit_profile_path(id: current_user.id) %>
</button>
  <% end %>
