This is the create a new review form!
<br/>
<% if @errors != nil %>
  <% @errors.each do |e| %>
    <%= e %>
  <% end %>
<% end %>

<%= form_for [@profile, @review] do |f| %>
  You are reviewing: <%= @reviewee.username %> for trip:
  <%= f.select :trip_id, options_for_select(@common_trips) %>
  <br/>
  <br/>
  <%= f.label "Please leave an honest review" %>
  <%= f.text_field :content %>
  <br/>
  <br/>
  <%= f.label "How would you rate this person?" %><br/>
  1<%= f.range_field :rating, in: 1..10 %>10<br/><br/>
  <%= f.button :submit %>
<% end %>
